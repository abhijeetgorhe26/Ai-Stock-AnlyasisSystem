# Models Layer

The `models` folder contains data structures, database schemas (e.g., Mongoose models for MongoDB), and data access abstractions.
Controllers invoke models to perform CRUD operations or fetch domain entities.
