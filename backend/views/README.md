# Views Layer

In a decoupled architecture with a React frontend, the `views` folder can store custom JSON response view formatters, email templates, or server-rendered layouts if needed.
Business logic is strictly decoupled from this layer.
